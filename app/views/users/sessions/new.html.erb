<h5 class="font-weight-600 margin-40px-bottom sm-margin-15px-bottom letter-spacing-8">LOGIN</h5>
<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :wrapper => :vertical_form_no_margin) do |f| %>
  <%= f.input :email, label: false, placeholder: "Email Address" %>
  <%= f.input :password, label: false, placeholder: "Password" %>
  <div class="mb-4">
    <button type="submit" class="btn btn-light-gray btn-medium text-medium margin-30px-top sm-margin-three-top">Login</button>
  </div>
  <div class="col-lg-12 mb-45">
    <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%= link_to "Forgot your password?", new_password_path(resource_name) %><br />
    <% end %>
    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      No Account? <%= link_to "Create one", new_registration_path(resource_name) %>
    <% end %>
  </div>
<% end %>
