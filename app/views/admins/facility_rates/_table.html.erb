<div class="table-card">
	<table class="table table-hover table-striped align-middle mb-0">
		<thead class="table-light">
			<tr>
				<th class="text-center">ID</th>
				<th>Name</th>
				<th>Price</th>
				<th>Facility</th>
				<th>Position</th>
				<th>Action</th>
			</tr>
		</thead>
		<tbody>
			<% for facility_rate in @facility_rates %>
				<tr>
					<td class="text-center"><%= facility_rate.id %></td>
					<td>
						<h5 class="fs-14 mb-1"><%= link_to facility_rate.name, admins_facility_rate_path(facility_rate.id), class: "text-body" %></h5>
						<p class="text-muted mb-0"><%= [facility_rate.time.presence, facility_rate.access.presence].compact.join(" · ") %></p>
					</td>
					<td><%= facility_rate.price %></td>
					<td><%= blank_or_not(facility_rate.facility&.name) %></td>
					<td><%= facility_rate.position %></td>
					<td>
						<div class="dropdown">
							<button class="btn btn-soft-secondary btn-sm dropdown" type="button" data-bs-toggle="dropdown" aria-expanded="false">
								<i class="ri-more-fill"></i>
							</button>
							<ul class="dropdown-menu dropdown-menu-end">
								<li>
									<%= link_to admins_facility_rate_path(facility_rate.id), class: "dropdown-item" do %>
										<i class="ri-eye-fill align-bottom me-2 text-muted"></i> View
									<% end %>
								</li>
								<li>
									<%= link_to edit_admins_facility_rate_path(facility_rate.id), class: "dropdown-item edit-list" do %>
										<i class="ri-pencil-fill align-bottom me-2 text-muted"></i> Edit
									<% end %>
								</li>
								<li class="dropdown-divider"></li>
								<li>
									<%= link_to admins_facility_rate_path(facility_rate.id), :data => { :confirm => 'Are you sure?' }, :method => :delete, class: "dropdown-item remove-list" do %>
										<i class="ri-delete-bin-fill align-bottom me-2 text-muted"></i> Delete
									<% end %>
								</li>
							</ul>
						</div>
					</td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>
<!-- pagination -->
<%= paginate @facility_rates %>
