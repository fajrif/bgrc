<% title_page("Menu Category Details") %>
<div class="row">
	<div class="col-md-12 col-sm-12">
		<div class="card card-box">
			<div class="card-body">
        <div>
          <%= render "details" %>
        </div>
				<div class="form-actions">
					<%= link_to 'Edit', edit_admins_menu_category_path(@menu_category.id), :class => 'btn btn-secondary' %>
					<%= link_to 'Destroy', admins_menu_category_path(@menu_category.id), :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger' %>
					<%= link_to 'View All', admins_menu_categories_path, :class => 'btn btn-light' %>
				</div>
			</div>
		</div>
	</div>
</div>
