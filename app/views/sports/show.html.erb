<%= render "home/banner" %>
<!-- start story section -->
<section class="wow animate__fadeIn">
    <div class="container">
        <div class="row align-items-start">
            <div class="col-lg-12 col-md-12 text-center text-md-start padding-three-lr wow animate__fadeInLeft" data-wow-delay="0.1s">
              <h5 class="alt-font text-extra-dark-gray mb-4"><%= @sport.name %></h5>
              <p><%= @sport.short_description %></p>
              <p><%= @sport.description %></p>
            </div>
        </div>
    </div>
</section>
<!-- end story section -->
